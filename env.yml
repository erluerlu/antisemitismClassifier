name: antisemitism-nli
channels: [conda-forge, defaults]
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.2
      - transformers>=4.44
      - datasets
      - evaluate
      - scikit-learn
      - accelerate
      - sentencepiece
      - pandas
      - numpy
      - emoji